apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "server-public.fullname" . }}-test-connection"
  labels:
    {{- include "server-public.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "server-public.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
