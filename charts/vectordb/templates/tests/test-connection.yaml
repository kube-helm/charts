apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vectordb.fullname" . }}-test-connection"
  labels:
    {{- include "vectordb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vectordb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
